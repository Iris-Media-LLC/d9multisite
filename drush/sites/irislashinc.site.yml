local:
  paths:
    drush: ${env.cwd}/vendor/bin/drush
    files: ${env.cwd}/docroot/sites/irislashinc/files
  root: '${env.cwd}/docroot'
  uri: 'http://irislashinc.lndo.site'

prod:
  paths:
    drush: ${env.cwd}/vendor/bin/drush
    files: ${env.cwd}/docroot/sites/irislashinc/files
  root: '${env.cwd}/docroot'
  uri: 'http://irislashinc.com'